# Postupná implementace: Knihovna promptů s editací system + user promptů

## Cíl
Do Knihovny promptů (📚 Knihovna promptů) přidat editaci Systémového a Uživatelského promptu ve stylu existujícího modalu "📝 Upravit AI prompt (systém + uživatelský)" - dva textareas vedle sebe a tlačítka Obnovit/Uložit.

--- ##

## KROK 1: HTML struktura (strong_translator.html)

V modálu `promptLibraryModal` (řádek ~637) nahradit:

```html
<!-- Současné -->
<div class="prompt-library-editor-wrap">
  <textarea id="promptLibraryEditor" ...></textarea>
</div>
```

Nové (přidat místo toho nebo pod to):

```html
<!-- Volitelné: ponechat stávající editor pro kompatibilitu, nebo odstranit -->
<div class="prompt-library-editor-wrap" style="display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-top: 12px;">
  <div>
    <label for="librarySystemPrompt" style="font-size: 0.79rem; color: var(--txt2); display: block; margin-bottom: 4px;">Systémový prompt (role: system)</label>
    <textarea id="librarySystemPrompt" class="prompt-library-editor" style="min-height: 100px; font-size: 12px;" spellcheck="false"></textarea>
  </div>
  <div>
    <label for="libraryUserPrompt" style="font-size: 0.79rem; color: var(--txt2); display: block; margin-bottom: 4px;">Uživatelský prompt (role: user)</label>
    <textarea id="libraryUserPrompt" class="prompt-library-editor" style="min-height: 100px; font-size: 12px;" spellcheck="false"></textarea>
  </div>
</div>
<div class="prompt-actions" style="margin-top: 8px; gap: 8px;">
  <button type="button" class="prompt-btn" onclick="restoreLibraryPrompts()" title="Obnovit výchozí">🔄 Obnovit výchozí</button>
  <button type="button" class="prompt-btn ok" onclick="saveLibraryPrompts()" title="Uložit prompt">💾 Uložit prompt</button>
  <span id="libraryPromptStatus" style="font-size: 0.78rem; color: var(--grn); margin-left: 4px;"></span>
</div>
```

--- ##

## KROK 2: Funkce pro save/reset (js/main.js)

Přidat funkce (např. po řádku 3764, za `saveEditedPrompt`):

```javascript
function restoreLibraryPrompts() {
  const sysEl = document.getElementById('librarySystemPrompt');
  const userEl = document.getElementById('libraryUserPrompt');
  const status = document.getElementById('libraryPromptStatus');
  
  if (sysEl) sysEl.value = getActiveSystemMessage();
  if (userEl) userEl.value = getActiveMainPromptTemplate('batch');
  
  if (status) {
    status.textContent = '↺ Obnoveno výchozí';
    status.style.color = 'var(--grn)';
    setTimeout(() => { if (status) status.textContent = ''; }, 2200);
  }
}

function saveLibraryPrompts() {
  const sysEl = document.getElementById('librarySystemPrompt');
  const userEl = document.getElementById('libraryUserPrompt');
  const status = document.getElementById('libraryPromptStatus');
  
  if (!sysEl || !userEl) return;
  
  const sysVal = (sysEl.value || '').trim();
  const userVal = (userEl.value || '').trim();
  
  if (!sysVal) {
    if (status) { status.textContent = '? Systémový prompt nesmí být prázdný'; status.style.color = 'var(--red)'; }
    return;
  }
  if (!userVal) {
    if (status) { status.textContent = '? Uživatelský prompt nesmí být prázdný'; status.style.color = 'var(--red)'; }
    return;
  }
  
  // Uložit
  localStorage.setItem('strong_custom_system_prompt', sysVal);
  setMainPrompt(userVal, 'custom');
  updatePromptStatusIndicator();
  
  if (status) {
    status.textContent = '✓ Uloženo';
    status.style.color = 'var(--grn)';
  }
  showToast('? AI prompt uložen');
  
  setTimeout(() => { if (status) status.textContent = ''; }, 2000);
}
```

--- ##

## KROK 3: Inicializace při otevření knihovny

V `showPromptLibraryModal` funkci (v promptLibrary.js) přidat na začátek po řádku 169:

```javascript
// Načíst aktuální hodnoty do editorů
const sysLib = document.getElementById('librarySystemPrompt');
const userLib = document.getElementById('libraryUserPrompt');
if (sysLib) sysLib.value = getActiveSystemMessage();
if (userLib) userLib.value = getActiveMainPromptTemplate('batch');
```

--- ##

## KROK 4: Expose pro inline handlery

V main.js na konci (kde jsou ostatní `window.xxx = xxx`) přidat:

```javascript
window.restoreLibraryPrompts = restoreLibraryPrompts;
window.saveLibraryPrompts = saveLibraryPrompts;
```

--- ##

## KROK 5: Testovací checklist

- [ ] Otevřít Knihovnu promptů - vidět dva textareas
- [ ] Zadat do nich text, kliknout Uložit - zkontrolovat localStorage (`strong_custom_system_prompt` a `strong_prompt`)
- [ ] Kliknout Obnovit výchozí - vidět default hodnoty
- [ ] Zavřít a znovu otevřít knihovnu - hodnoty jsou zachovány
- [ ] Otevřít samostatný modal "Upravit AI prompt" - ověřit, že funguje nezávisle
- [ ] Otestovat s různými AI providery

--- ##

## Poznámky

- Používají se stejné storage klíče jako existující modal:
  - `strong_custom_system_prompt` pro systémový prompt
  - `strong_prompt` + `strong_prompt_mode='custom'` pro uživatelský prompt
- Funkce `getActiveSystemMessage()` a `getActiveMainPromptTemplate()` už existují v kódu
- `setMainPrompt()` a `updatePromptStatusIndicator()` už také existují
- Editor pro jednotlivé prompty v knihovně zůstává zachován (není mazán) - je to jen doplněk